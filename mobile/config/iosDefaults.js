/**
 * Minimum iOS version the app supports — keep aligned with the latest major iOS you ship for.
 * Bump when targeting a newer iOS release (requires matching Xcode SDK on CI/local).
 */
const LATEST_IOS_DEPLOYMENT_TARGET = "26.0";

module.exports = { LATEST_IOS_DEPLOYMENT_TARGET };
