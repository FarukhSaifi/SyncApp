/**
 * Client-side route paths (App Router).
 */
export const ROUTES = Object.freeze({
  DASHBOARD: "/",
  EDITOR: "/editor",
  SETTINGS: "/settings",
  PROFILE: "/profile",
  USERS: "/users",
  LOGIN: "/login",
  REGISTER: "/register",
  ANALYTICS: "/analytics",
} as const);
