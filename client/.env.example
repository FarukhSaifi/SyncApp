# API Configuration
# Backend API URL (required for production, optional in development with proxy)
# In development, Vite proxy automatically forwards /api to http://localhost:9000
# In production, set this to your deployed backend URL (e.g., https://api.example.com/api)
VITE_API_BACKEND_URL=http://localhost:9000/api

# App Configuration (optional)
VITE_APP_NAME=SyncApp
VITE_APP_VERSION=1.0.0
