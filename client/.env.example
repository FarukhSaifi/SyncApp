# API Configuration
# Backend API URL (required for production, optional in development with proxy)
# 
# Development: 
# - In development, Vite proxy automatically forwards /api to http://localhost:9000
# - You can optionally set this if your backend runs on a different port
VITE_API_BACKEND_URL=http://localhost:9000/api
#
# Production (Vercel):
# - Set this in Vercel Dashboard → Project Settings → Environment Variables
# - Use your production backend URL (must include /api suffix)
# - Example: VITE_API_BACKEND_URL=https://sync-app-server.railway.app/api
# - Example: VITE_API_BACKEND_URL=https://api.example.com/api
#
# You can set different values for Production, Preview, and Development environments in Vercel
# Just add the same variable name with different values for each environment

# App Configuration (optional)
VITE_APP_NAME=SyncApp
VITE_APP_VERSION=1.0.0
