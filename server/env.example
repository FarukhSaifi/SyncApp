# Environment Variables for SyncApp Server

# Server Configuration
NODE_ENV=development
PORT=9000

# Database Configuration (Required)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/syncapp

# Security (Required)
JWT_SECRET=your_secure_random_string_here_min_32_chars

# Encryption Keys (Required - generate with: node ../scripts/generate-keys.js)
ENCRYPTION_KEY=your_32_byte_hex_string_here
ENCRYPTION_IV=your_16_byte_hex_string_here

# CORS Configuration (Optional - comma-separated for multiple origins)
# Defaults to http://localhost:3000 in development
CORS_ORIGIN=http://localhost:3000,https://sync-app-client.vercel.app/
# Example for multiple origins: CORS_ORIGIN=http://localhost:3000,https://sync-app-client.vercel.app/

# Rate Limiting (Optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AI (Google Vertex AI – Gemini) – Optional. Required for AI Assistant in Editor.
# 1. Create a Google Cloud project and enable Vertex AI API: https://console.cloud.google.com/apis/library/aiplatform.googleapis.com
# 2. Use a service account JSON key path, or run: gcloud auth application-default login
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
# Path to service account JSON (optional if using gcloud default credentials)
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
# Optional: model override (default: gemini-2.0-flash-001). See: https://cloud.google.com/vertex-ai/generative-ai/docs/learn/model-versions
# GOOGLE_AI_MODEL=gemini-2.0-flash-001
# Optional: disable Google Search grounding for outline step (default: enabled for SEO)
# AI_USE_GOOGLE_SEARCH_RETRIEVAL=false
